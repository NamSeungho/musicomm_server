/* Middleware module dependencies */
const app = require('../app');

/* Create HTTP server */
const PORT = 3000;
const server = app.listen(require('../const').PORT, () => {
    console.log('Open HTTP server on ' + PORT + ' port');
});

/* Socket module dependencies */
require('../socket')(server);